# Generated by scripts/gen-tenants.py — edit the table there, not this file.
apiVersion: v1
kind: Namespace
metadata:
  name: tenants
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: avalanche-analytics-bi
  namespace: tenants
  labels: {app: avalanche, tenant: analytics, team: bi}
spec:
  replicas: 1
  selector:
    matchLabels: {app: avalanche, tenant: analytics, team: bi}
  template:
    metadata:
      labels: {app: avalanche, tenant: analytics, team: bi}
    spec:
      containers:
        - name: avalanche
          image: quay.io/prometheuscommunity/avalanche:v0.7.0
          args:
            # 20 x 400 = 8000 active series per replica
            - --gauge-metric-count=20
            - --series-count=400
            - --label-count=5
            - --value-interval=15
            # Hourly series_id churn: enough to exercise idle-series
            # expiry without making active series a moving target.
            - --series-interval=3600
            - --metric-interval=0
            - --port=9001
          ports:
            - {name: metrics, containerPort: 9001}
          resources:
            requests: {cpu: 10m, memory: 32Mi}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: avalanche-analytics-ml
  namespace: tenants
  labels: {app: avalanche, tenant: analytics, team: ml}
spec:
  replicas: 1
  selector:
    matchLabels: {app: avalanche, tenant: analytics, team: ml}
  template:
    metadata:
      labels: {app: avalanche, tenant: analytics, team: ml}
    spec:
      containers:
        - name: avalanche
          image: quay.io/prometheuscommunity/avalanche:v0.7.0
          args:
            # 10 x 500 = 5000 active series per replica
            - --gauge-metric-count=10
            - --series-count=500
            - --label-count=5
            - --value-interval=15
            # Hourly series_id churn: enough to exercise idle-series
            # expiry without making active series a moving target.
            - --series-interval=3600
            - --metric-interval=0
            - --port=9001
          ports:
            - {name: metrics, containerPort: 9001}
          resources:
            requests: {cpu: 10m, memory: 32Mi}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: avalanche-infra-sre
  namespace: tenants
  labels: {app: avalanche, tenant: infra, team: sre}
spec:
  replicas: 1
  selector:
    matchLabels: {app: avalanche, tenant: infra, team: sre}
  template:
    metadata:
      labels: {app: avalanche, tenant: infra, team: sre}
    spec:
      containers:
        - name: avalanche
          image: quay.io/prometheuscommunity/avalanche:v0.7.0
          args:
            # 20 x 350 = 7000 active series per replica
            - --gauge-metric-count=20
            - --series-count=350
            - --label-count=5
            - --value-interval=15
            # Hourly series_id churn: enough to exercise idle-series
            # expiry without making active series a moving target.
            - --series-interval=3600
            - --metric-interval=0
            - --port=9001
          ports:
            - {name: metrics, containerPort: 9001}
          resources:
            requests: {cpu: 10m, memory: 32Mi}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: avalanche-payments
  namespace: tenants
  labels: {app: avalanche, tenant: payments, team: payments}
spec:
  replicas: 1
  selector:
    matchLabels: {app: avalanche, tenant: payments, team: payments}
  template:
    metadata:
      labels: {app: avalanche, tenant: payments, team: payments}
    spec:
      containers:
        - name: avalanche
          image: quay.io/prometheuscommunity/avalanche:v0.7.0
          args:
            # 10 x 120 = 1200 active series per replica
            - --gauge-metric-count=10
            - --series-count=120
            - --label-count=5
            - --value-interval=15
            # Hourly series_id churn: enough to exercise idle-series
            # expiry without making active series a moving target.
            - --series-interval=3600
            - --metric-interval=0
            - --port=9001
          ports:
            - {name: metrics, containerPort: 9001}
          resources:
            requests: {cpu: 10m, memory: 32Mi}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: avalanche-platform
  namespace: tenants
  labels: {app: avalanche, tenant: platform, team: platform}
spec:
  replicas: 1
  selector:
    matchLabels: {app: avalanche, tenant: platform, team: platform}
  template:
    metadata:
      labels: {app: avalanche, tenant: platform, team: platform}
    spec:
      containers:
        - name: avalanche
          image: quay.io/prometheuscommunity/avalanche:v0.7.0
          args:
            # 2 x 50 = 100 active series per replica
            - --gauge-metric-count=2
            - --series-count=50
            - --label-count=5
            - --value-interval=15
            # Hourly series_id churn: enough to exercise idle-series
            # expiry without making active series a moving target.
            - --series-interval=3600
            - --metric-interval=0
            - --port=9001
          ports:
            - {name: metrics, containerPort: 9001}
          resources:
            requests: {cpu: 10m, memory: 32Mi}
