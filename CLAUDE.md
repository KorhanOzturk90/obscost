# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**Read `AGENTS.md`, not this file.** It's the single, actively-maintained source of truth for repository status, architecture, commands, and (importantly) how to work safely alongside the other agent sessions that are usually also active in this repo. This file is kept only as a short pointer, since Claude Code looks for `CLAUDE.md` specifically — don't duplicate content back into it; edit `AGENTS.md` instead.
